export interface DefaultResponse {
  totalCount: number;
  count: number;
  status: number;
  success: boolean;
}

export interface ReportResponse {
  data: any[]
  status: number;
  success: boolean;
}